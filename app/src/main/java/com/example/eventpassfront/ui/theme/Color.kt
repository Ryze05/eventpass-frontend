package com.example.eventpassfront.ui.theme

import androidx.compose.ui.graphics.Color

// Fondo Principal (Píxeles apagados)
val OledBlack = Color(0xFF000000)

// Acento Principal (Naranja vibrante)
val DeepOrange = Color(0xFFFF6D00)
val LightOrange = Color(0xFFFF9E40) // Para estados "hover" o degradados

// Superficies (Para que las tarjetas se vean sobre el fondo negro)
val SurfaceDark = Color(0xFF121212) // Gris muy oscuro
val SurfaceVariant = Color(0xFF1E1E1E) // Un poco más claro para líneas de separación

// Textos
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFB3B3B3) // Gris para fechas o descripciones

// Errores (Un rojo que combine con el naranja)
val ErrorRed = Color(0xFFCF6679)